---
name: Lint
# v8.5.0
# https://github.com/github/super-linter/releases/tag/v8.5.0

on:
  workflow_call:
    inputs:
      VALIDATE_ANSIBLE:
        required: false
        type: string
      VALIDATE_ARM:
        required: false
        type: string
      VALIDATE_BASH:
        required: false
        type: string
      VALIDATE_BASH_EXEC:
        required: false
        type: string
      VALIDATE_BIOME_FORMAT:
        required: false
        type: string
      VALIDATE_BIOME_LINT:
        required: false
        type: string
      VALIDATE_CPP:
        required: false
        type: string
      VALIDATE_CHECKOV:
        required: false
        type: string
        default: "false"
      VALIDATE_CLANG_FORMAT:
        required: false
        type: string
      VALIDATE_CLOJURE:
        required: false
        type: string
      VALIDATE_CLOUDFORMATION:
        required: false
        type: string
      VALIDATE_COFFEESCRIPT:
        required: false
        type: string
      VALIDATE_CSHARP:
        required: false
        type: string
      VALIDATE_CSS:
        required: false
        type: string
      VALIDATE_CSS_PRETTIER:
        required: false
        type: string
      VALIDATE_DART:
        required: false
        type: string
      VALIDATE_DOCKERFILE_HADOLINT:
        required: false
        type: string
      VALIDATE_DOTNET_SLN_FORMAT_ANALYZERS:
        required: false
        type: string
      VALIDATE_DOTNET_SLN_FORMAT_STYLE:
        required: false
        type: string
      VALIDATE_DOTNET_SLN_FORMAT_WHITESPACE:
        required: false
        type: string
      VALIDATE_EDITORCONFIG:
        required: false
        type: string
      VALIDATE_ENV:
        required: false
        type: string
      VALIDATE_GIT_COMMITLINT:
        required: false
        type: string
      VALIDATE_GIT_MERGE_CONFLICT_MARKERS:
        required: false
        type: string
      VALIDATE_GHERKIN:
        required: false
        type: string
      VALIDATE_GITHUB_ACTIONS:
        required: false
        type: string
      VALIDATE_GITHUB_ACTIONS_ZIZMOR:
        required: false
        type: string
      VALIDATE_GITLEAKS:
        required: false
        type: string
      VALIDATE_GO:
        required: false
        type: string
      VALIDATE_GO_MODULES:
        required: false
        type: string
      VALIDATE_GO_RELEASER:
        required: false
        type: string
      VALIDATE_GOOGLE_JAVA_FORMAT:
        required: false
        type: string
      VALIDATE_GRAPHQL_PRETTIER:
        required: false
        type: string
      VALIDATE_GROOVY:
        required: false
        type: string
      VALIDATE_HTML:
        required: false
        type: string
      VALIDATE_HTML_PRETTIER:
        required: false
        type: string
      VALIDATE_JAVA:
        required: false
        type: string
      VALIDATE_JAVASCRIPT_ES:
        required: false
        type: string
      VALIDATE_JAVASCRIPT_PRETTIER:
        required: false
        type: string
        default: "false"
      VALIDATE_JAVASCRIPT_STANDARD:
        required: false
        type: string
        default: "false"
      VALIDATE_JSCPD:
        required: false
        type: string
        default: "false"
      VALIDATE_JSON:
        required: false
        type: string
      VALIDATE_JSON_PRETTIER:
        required: false
        type: string
        default: "false"
      VALIDATE_JSONC:
        required: false
        type: string
      VALIDATE_JSONC_PRETTIER:
        required: false
        type: string
      VALIDATE_JSX:
        required: false
        type: string
      VALIDATE_JSX_PRETTIER:
        required: false
        type: string
      VALIDATE_JUPYTER_NBQA_BLACK:
        required: false
        type: string
      VALIDATE_JUPYTER_NBQA_FLAKE8:
        required: false
        type: string
      VALIDATE_JUPYTER_NBQA_ISORT:
        required: false
        type: string
      VALIDATE_JUPYTER_NBQA_MYPY:
        required: false
        type: string
      VALIDATE_JUPYTER_NBQA_PYLINT:
        required: false
        type: string
      VALIDATE_JUPYTER_NBQA_RUFF:
        required: false
        type: string
      VALIDATE_KOTLIN:
        required: false
        type: string
      VALIDATE_KOTLIN_ANDROID:
        required: false
        type: string
      VALIDATE_KUBERNETES_KUBECONFORM:
        required: false
        type: string
      VALIDATE_LATEX:
        required: false
        type: string
      VALIDATE_LUA:
        required: false
        type: string
      VALIDATE_MARKDOWN:
        required: false
        type: string
      VALIDATE_MARKDOWN_PRETTIER:
        required: false
        type: string
        default: "false"
      VALIDATE_NATURAL_LANGUAGE:
        required: false
        type: string
      VALIDATE_OPENAPI:
        required: false
        type: string
      VALIDATE_PERL:
        required: false
        type: string
      VALIDATE_PHP:
        required: false
        type: string
      VALIDATE_PHP_BUILTIN:
        required: false
        type: string
      VALIDATE_PHP_PHPCS:
        required: false
        type: string
      VALIDATE_PHP_PHPSTAN:
        required: false
        type: string
      VALIDATE_PHP_PSALM:
        required: false
        type: string
      VALIDATE_PRE_COMMIT:
        required: false
        type: string
      VALIDATE_POWERSHELL:
        required: false
        type: string
        default: "false"
      VALIDATE_PROTOBUF:
        required: false
        type: string
      VALIDATE_PYTHON:
        required: false
        type: string
      VALIDATE_PYTHON_BLACK:
        required: false
        type: string
      VALIDATE_PYTHON_FLAKE8:
        required: false
        type: string
      VALIDATE_PYTHON_ISORT:
        required: false
        type: string
      VALIDATE_PYTHON_MYPY:
        required: false
        type: string
        default: "false"
      VALIDATE_PYTHON_PYLINT:
        required: false
        type: string
      VALIDATE_PYTHON_RUFF:
        required: false
        type: string
      VALIDATE_PYTHON_RUFF_FORMAT:
        required: false
        type: string
      VALIDATE_R:
        required: false
        type: string
      VALIDATE_RAKU:
        required: false
        type: string
      VALIDATE_RENOVATE:
        required: false
        type: string
      VALIDATE_RUBY:
        required: false
        type: string
      VALIDATE_RUST_2015:
        required: false
        type: string
      VALIDATE_RUST_2018:
        required: false
        type: string
      VALIDATE_RUST_2021:
        required: false
        type: string
      VALIDATE_RUST_2024:
        required: false
        type: string
      VALIDATE_RUST_CLIPPY:
        required: false
        type: string
      VALIDATE_SCALAFMT:
        required: false
        type: string
      VALIDATE_SHELL_SHFMT:
        required: false
        type: string
        default: "false"
      VALIDATE_SNAKEMAKE_LINT:
        required: false
        type: string
      VALIDATE_SNAKEMAKE_SNAKEFMT:
        required: false
        type: string
      VALIDATE_SPELL_CODESPELL:
        required: false
        type: string
      VALIDATE_STATES:
        required: false
        type: string
      VALIDATE_SQL:
        required: false
        type: string
      VALIDATE_SQLFLUFF:
        required: false
        type: string
      VALIDATE_TEKTON:
        required: false
        type: string
      VALIDATE_TERRAFORM_FMT:
        required: false
        type: string
      VALIDATE_TERRAFORM_TERRASCAN:
        required: false
        type: string
      VALIDATE_TERRAFORM_TFLINT:
        required: false
        type: string
      VALIDATE_TERRAGRUNT:
        required: false
        type: string
      VALIDATE_TRIVY:
        required: false
        type: string
      VALIDATE_TSX:
        required: false
        type: string
      VALIDATE_TYPESCRIPT_ES:
        required: false
        type: string
      VALIDATE_TYPESCRIPT_STANDARD:
        required: false
        type: string
      VALIDATE_TYPESCRIPT_PRETTIER:
        required: false
        type: string
      VALIDATE_VUE:
        required: false
        type: string
      VALIDATE_VUE_PRETTIER:
        required: false
        type: string
      VALIDATE_XML:
        required: false
        type: string
      VALIDATE_YAML:
        required: false
        type: string
      VALIDATE_YAML_PRETTIER:
        required: false
        type: string
        default: "false"
      VALIDATE_ALL_CODEBASE:
        required: false
        type: string
    # secrets:
    #   token:
    #     required: true

env:
  GIT_DISCOVERY_ACROSS_FILESYSTEM: 1

jobs:
  Lint:
    name: Lint Code Base
    runs-on: ubuntu-22.04 # ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          # super-linter needs the full git history to get the
          # list of files that changed across commits
          fetch-depth: 0

      - name: Checkout dennykorsukewitz
        uses: actions/checkout@v6
        with:
          fetch-depth: "1"
          repository: dennykorsukewitz/dennykorsukewitz
          ref: "dev"
          path: linters

      - name: Prepare linters config
        run: cp -R "${GITHUB_WORKSPACE}"/linters/.github/linters "${GITHUB_WORKSPACE}"/.github/

      - name: Remove dennykorsukewitz directory
        run: rm -Rf "${GITHUB_WORKSPACE}"/linters

      - name: Check GITHUB_WORKSPACE
        run: |
          echo "GITHUB_WORKSPACE: ${GITHUB_WORKSPACE}"
          echo "repository: ${GITHUB_WORKSPACE}/repository"
          echo "dennykorsukewitz: ${GITHUB_WORKSPACE}/dennykorsukewitz"
          ls -lahi "${GITHUB_WORKSPACE}"

      - name: Branch Name
        run: echo running on branch "${GITHUB_REF##*/}"

      - name: Show linters config
        run: ls -alih "${GITHUB_WORKSPACE}/.github/linters"

      - name: Set environment variables
        run: |
          if [[ "${{ inputs.VALIDATE_ANSIBLE }}" == "false" ]]; then
            echo "VALIDATE_ANSIBLE=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_ARM }}" == "false" ]]; then
            echo "VALIDATE_ARM=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_BASH }}" == "false" ]]; then
            echo "VALIDATE_BASH=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_BASH_EXEC }}" == "false" ]]; then
            echo "VALIDATE_BASH_EXEC=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_BIOME_FORMAT }}" == "false" ]]; then
            echo "VALIDATE_BIOME_FORMAT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_BIOME_LINT }}" == "false" ]]; then
            echo "VALIDATE_BIOME_LINT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_CPP }}" == "false" ]]; then
            echo "VALIDATE_CPP=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_CHECKOV }}" == "false" ]]; then
            echo "VALIDATE_CHECKOV=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_CLANG_FORMAT }}" == "false" ]]; then
            echo "VALIDATE_CLANG_FORMAT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_CLOJURE }}" == "false" ]]; then
            echo "VALIDATE_CLOJURE=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_CLOUDFORMATION }}" == "false" ]]; then
            echo "VALIDATE_CLOUDFORMATION=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_COFFEESCRIPT }}" == "false" ]]; then
            echo "VALIDATE_COFFEESCRIPT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_CSHARP }}" == "false" ]]; then
            echo "VALIDATE_CSHARP=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_CSS }}" == "false" ]]; then
            echo "VALIDATE_CSS=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_CSS_PRETTIER }}" == "false" ]]; then
            echo "VALIDATE_CSS_PRETTIER=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_DART }}" == "false" ]]; then
            echo "VALIDATE_DART=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_DOCKERFILE_HADOLINT }}" == "false" ]]; then
            echo "VALIDATE_DOCKERFILE_HADOLINT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_DOTNET_SLN_FORMAT_ANALYZERS }}" == "false" ]]; then
            echo "VALIDATE_DOTNET_SLN_FORMAT_ANALYZERS=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_DOTNET_SLN_FORMAT_STYLE }}" == "false" ]]; then
            echo "VALIDATE_DOTNET_SLN_FORMAT_STYLE=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_DOTNET_SLN_FORMAT_WHITESPACE }}" == "false" ]]; then
            echo "VALIDATE_DOTNET_SLN_FORMAT_WHITESPACE=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_EDITORCONFIG }}" == "false" ]]; then
            echo "VALIDATE_EDITORCONFIG=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_ENV }}" == "false" ]]; then
            echo "VALIDATE_ENV=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_GIT_COMMITLINT }}" == "false" ]]; then
            echo "VALIDATE_GIT_COMMITLINT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_GIT_MERGE_CONFLICT_MARKERS }}" == "false" ]]; then
            echo "VALIDATE_GIT_MERGE_CONFLICT_MARKERS=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_GHERKIN }}" == "false" ]]; then
            echo "VALIDATE_GHERKIN=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_GITHUB_ACTIONS }}" == "false" ]]; then
            echo "VALIDATE_GITHUB_ACTIONS=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_GITHUB_ACTIONS_ZIZMOR }}" == "false" ]]; then
            echo "VALIDATE_GITHUB_ACTIONS_ZIZMOR=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_GITLEAKS }}" == "false" ]]; then
            echo "VALIDATE_GITLEAKS=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_GO }}" == "false" ]]; then
            echo "VALIDATE_GO=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_GO_MODULES }}" == "false" ]]; then
            echo "VALIDATE_GO_MODULES=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_GO_RELEASER }}" == "false" ]]; then
            echo "VALIDATE_GO_RELEASER=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_GOOGLE_JAVA_FORMAT }}" == "false" ]]; then
            echo "VALIDATE_GOOGLE_JAVA_FORMAT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_GRAPHQL_PRETTIER }}" == "false" ]]; then
            echo "VALIDATE_GRAPHQL_PRETTIER=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_GROOVY }}" == "false" ]]; then
            echo "VALIDATE_GROOVY=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_HTML }}" == "false" ]]; then
            echo "VALIDATE_HTML=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_HTML_PRETTIER }}" == "false" ]]; then
            echo "VALIDATE_HTML_PRETTIER=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JAVA }}" == "false" ]]; then
            echo "VALIDATE_JAVA=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JAVASCRIPT_ES }}" == "false" ]]; then
            echo "VALIDATE_JAVASCRIPT_ES=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JAVASCRIPT_PRETTIER }}" == "false" ]]; then
            echo "VALIDATE_JAVASCRIPT_PRETTIER=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JAVASCRIPT_STANDARD }}" == "false" ]]; then
            echo "VALIDATE_JAVASCRIPT_STANDARD=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JSCPD }}" == "false" ]]; then
            echo "VALIDATE_JSCPD=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JSON }}" == "false" ]]; then
            echo "VALIDATE_JSON=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JSON_PRETTIER }}" == "false" ]]; then
            echo "VALIDATE_JSON_PRETTIER=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JSONC }}" == "false" ]]; then
            echo "VALIDATE_JSONC=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JSONC_PRETTIER }}" == "false" ]]; then
            echo "VALIDATE_JSONC_PRETTIER=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JSX }}" == "false" ]]; then
            echo "VALIDATE_JSX=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JSX_PRETTIER }}" == "false" ]]; then
            echo "VALIDATE_JSX_PRETTIER=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JUPYTER_NBQA_BLACK }}" == "false" ]]; then
            echo "VALIDATE_JUPYTER_NBQA_BLACK=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JUPYTER_NBQA_FLAKE8 }}" == "false" ]]; then
            echo "VALIDATE_JUPYTER_NBQA_FLAKE8=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JUPYTER_NBQA_ISORT }}" == "false" ]]; then
            echo "VALIDATE_JUPYTER_NBQA_ISORT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JUPYTER_NBQA_MYPY }}" == "false" ]]; then
            echo "VALIDATE_JUPYTER_NBQA_MYPY=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JUPYTER_NBQA_PYLINT }}" == "false" ]]; then
            echo "VALIDATE_JUPYTER_NBQA_PYLINT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_JUPYTER_NBQA_RUFF }}" == "false" ]]; then
            echo "VALIDATE_JUPYTER_NBQA_RUFF=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_KOTLIN }}" == "false" ]]; then
            echo "VALIDATE_KOTLIN=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_KOTLIN_ANDROID }}" == "false" ]]; then
            echo "VALIDATE_KOTLIN_ANDROID=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_KUBERNETES_KUBECONFORM }}" == "false" ]]; then
            echo "VALIDATE_KUBERNETES_KUBECONFORM=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_LATEX }}" == "false" ]]; then
            echo "VALIDATE_LATEX=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_LUA }}" == "false" ]]; then
            echo "VALIDATE_LUA=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_MARKDOWN }}" == "false" ]]; then
            echo "VALIDATE_MARKDOWN=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_MARKDOWN_PRETTIER }}" == "false" ]]; then
            echo "VALIDATE_MARKDOWN_PRETTIER=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_NATURAL_LANGUAGE }}" == "false" ]]; then
            echo "VALIDATE_NATURAL_LANGUAGE=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_OPENAPI }}" == "false" ]]; then
            echo "VALIDATE_OPENAPI=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PERL }}" == "false" ]]; then
            echo "VALIDATE_PERL=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PHP }}" == "false" ]]; then
            echo "VALIDATE_PHP=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PHP_BUILTIN }}" == "false" ]]; then
            echo "VALIDATE_PHP_BUILTIN=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PHP_PHPCS }}" == "false" ]]; then
            echo "VALIDATE_PHP_PHPCS=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PHP_PHPSTAN }}" == "false" ]]; then
            echo "VALIDATE_PHP_PHPSTAN=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PHP_PSALM }}" == "false" ]]; then
            echo "VALIDATE_PHP_PSALM=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PRE_COMMIT }}" == "false" ]]; then
            echo "VALIDATE_PRE_COMMIT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_POWERSHELL }}" == "false" ]]; then
            echo "VALIDATE_POWERSHELL=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PROTOBUF }}" == "false" ]]; then
            echo "VALIDATE_PROTOBUF=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PYTHON }}" == "false" ]]; then
            echo "VALIDATE_PYTHON=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PYTHON_BLACK }}" == "false" ]]; then
            echo "VALIDATE_PYTHON_BLACK=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PYTHON_FLAKE8 }}" == "false" ]]; then
            echo "VALIDATE_PYTHON_FLAKE8=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PYTHON_ISORT }}" == "false" ]]; then
            echo "VALIDATE_PYTHON_ISORT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PYTHON_MYPY }}" == "false" ]]; then
            echo "VALIDATE_PYTHON_MYPY=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PYTHON_PYLINT }}" == "false" ]]; then
            echo "VALIDATE_PYTHON_PYLINT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PYTHON_RUFF }}" == "false" ]]; then
            echo "VALIDATE_PYTHON_RUFF=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_PYTHON_RUFF_FORMAT }}" == "false" ]]; then
            echo "VALIDATE_PYTHON_RUFF_FORMAT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_R }}" == "false" ]]; then
            echo "VALIDATE_R=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_RAKU }}" == "false" ]]; then
            echo "VALIDATE_RAKU=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_RENOVATE }}" == "false" ]]; then
            echo "VALIDATE_RENOVATE=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_RUBY }}" == "false" ]]; then
            echo "VALIDATE_RUBY=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_RUST_2015 }}" == "false" ]]; then
            echo "VALIDATE_RUST_2015=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_RUST_2018 }}" == "false" ]]; then
            echo "VALIDATE_RUST_2018=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_RUST_2021 }}" == "false" ]]; then
            echo "VALIDATE_RUST_2021=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_RUST_2024 }}" == "false" ]]; then
            echo "VALIDATE_RUST_2024=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_RUST_CLIPPY }}" == "false" ]]; then
            echo "VALIDATE_RUST_CLIPPY=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_SCALAFMT }}" == "false" ]]; then
            echo "VALIDATE_SCALAFMT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_SHELL_SHFMT }}" == "false" ]]; then
            echo "VALIDATE_SHELL_SHFMT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_SNAKEMAKE_LINT }}" == "false" ]]; then
            echo "VALIDATE_SNAKEMAKE_LINT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_SNAKEMAKE_SNAKEFMT }}" == "false" ]]; then
            echo "VALIDATE_SNAKEMAKE_SNAKEFMT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_SPELL_CODESPELL }}" == "false" ]]; then
            echo "VALIDATE_SPELL_CODESPELL=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_STATES }}" == "false" ]]; then
            echo "VALIDATE_STATES=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_SQL }}" == "false" ]]; then
            echo "VALIDATE_SQL=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_SQLFLUFF }}" == "false" ]]; then
            echo "VALIDATE_SQLFLUFF=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_TEKTON }}" == "false" ]]; then
            echo "VALIDATE_TEKTON=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_TERRAFORM_FMT }}" == "false" ]]; then
            echo "VALIDATE_TERRAFORM_FMT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_TERRAFORM_TERRASCAN }}" == "false" ]]; then
            echo "VALIDATE_TERRAFORM_TERRASCAN=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_TERRAFORM_TFLINT }}" == "false" ]]; then
            echo "VALIDATE_TERRAFORM_TFLINT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_TERRAGRUNT }}" == "false" ]]; then
            echo "VALIDATE_TERRAGRUNT=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_TRIVY }}" == "false" ]]; then
            echo "VALIDATE_TRIVY=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_TSX }}" == "false" ]]; then
            echo "VALIDATE_TSX=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_TYPESCRIPT_ES }}" == "false" ]]; then
            echo "VALIDATE_TYPESCRIPT_ES=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_TYPESCRIPT_STANDARD }}" == "false" ]]; then
            echo "VALIDATE_TYPESCRIPT_STANDARD=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_TYPESCRIPT_PRETTIER }}" == "false" ]]; then
            echo "VALIDATE_TYPESCRIPT_PRETTIER=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_VUE }}" == "false" ]]; then
            echo "VALIDATE_VUE=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_VUE_PRETTIER }}" == "false" ]]; then
            echo "VALIDATE_VUE_PRETTIER=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_XML }}" == "false" ]]; then
            echo "VALIDATE_XML=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_YAML }}" == "false" ]]; then
            echo "VALIDATE_YAML=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_YAML_PRETTIER }}" == "false" ]]; then
            echo "VALIDATE_YAML_PRETTIER=false" >> "$GITHUB_ENV"
          fi
          if [[ "${{ inputs.VALIDATE_ALL_CODEBASE }}" == "false" ]]; then
            echo "VALIDATE_ALL_CODEBASE=false" >> "$GITHUB_ENV"
          fi

          echo "Environment variables set:"
          cat "$GITHUB_ENV"

      - name: Lint Code Base
        uses: super-linter/super-linter@v8.6.0
        env:
          GITHUB_TOKEN: "${secrets.GITHUB_TOKEN}"
          LINTER_RULES_PATH: "./.github/linters"
          # super-linter: USE_FIND_ALGORITHM=true only with full codebase; false when incremental (VALIDATE_ALL_CODEBASE=false).
          USE_FIND_ALGORITHM: ${{ inputs.VALIDATE_ALL_CODEBASE != 'false' }}
          FILTER_REGEX_EXCLUDE: "(.*.scss)"
          IGNORE_GITIGNORED_FILES: true
          DEFAULT_WORKSPACE: "${GITHUB_WORKSPACE}"

          # Test
          GIT_DISCOVERY_ACROSS_FILESYSTEM: 1

          # dotenv-linter: skip alphabetical key order (grouped keys in e.g. configs/instance/my.env)
          DOTENV_LINTER_IGNORE_CHECKS: UnorderedKey

          # Disable specific linters

